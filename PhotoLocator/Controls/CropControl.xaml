<UserControl x:Class="PhotoLocator.Controls.CropControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoLocator.Controls"
             mc:Ignorable="d" 
             PreviewMouseDown="HandleMouseDown" PreviewMouseUp="HandleMouseUp" PreviewMouseMove="HandleMouseMove"
             d:DataContext="{d:DesignInstance Type=local:CropControl, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800" UseLayoutRounding="True">
    <UserControl.Resources>
        <DrawingBrush x:Key="BorderFill" TileMode="Tile" Viewport="0,0,32,32" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Geometry="M0,0 H1 V1 H2 V2 H1 V1 H0Z" Brush="#40000000"/>
                    <GeometryDrawing Geometry="M1,0 H2 V1 H1 Z M0,1 H1 V2 H0 Z" Brush="#40FFFFFF"/>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Name="TopRow" Height="{Binding CropTopOffset, Mode=TwoWay}"/>
            <RowDefinition Name="MidRow" Height="{Binding CropHeight, Mode=TwoWay}"/>
            <RowDefinition Name="BottomRow" Height="{Binding CropBottomOffset, Mode=TwoWay}"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Name="LeftColumn" Width="{Binding CropLeftOffset, Mode=TwoWay}"/>
            <ColumnDefinition Name="MidColumn" Width="{Binding CropWidth, Mode=TwoWay}"/>
            <ColumnDefinition Name="RightColumn" Width="{Binding CropRightOffset, Mode=TwoWay}"/>
        </Grid.ColumnDefinitions>
        <Rectangle Grid.Column="0" Grid.Row="0" Fill="{StaticResource BorderFill}"/>
        <Rectangle Grid.Column="0" Grid.Row="1" Fill="{StaticResource BorderFill}"/>
        <Rectangle Grid.Column="0" Grid.Row="2" Fill="{StaticResource BorderFill}"/>
        <Rectangle Grid.Column="1" Grid.Row="0" Fill="{StaticResource BorderFill}"/>
        <Grid Grid.Column="1" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="0.382*"/>
                <RowDefinition Height="0.236*"/>
                <RowDefinition Height="0.382*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.382*"/>
                <ColumnDefinition Width="0.236*"/>
                <ColumnDefinition Width="0.382*"/>
            </Grid.ColumnDefinitions>
            <Rectangle Grid.Row="1" Grid.ColumnSpan="3" VerticalAlignment="Top" Fill="{StaticResource BorderFill}" Height="1" />
            <Rectangle Grid.Row="1" Grid.ColumnSpan="3" VerticalAlignment="Bottom" Fill="{StaticResource BorderFill}" Height="1" />
            <Rectangle Grid.Column="1" Grid.RowSpan="3" HorizontalAlignment="Left" Fill="{StaticResource BorderFill}" Width="1" />
            <Rectangle Grid.Column="1" Grid.RowSpan="3" HorizontalAlignment="Right" Fill="{StaticResource BorderFill}" Width="1" />
        </Grid>
        <Rectangle Grid.Column="1" Grid.Row="1" Fill="#01000000" Cursor="SizeAll" Tag="Move" />
        <Rectangle Grid.Column="1" Grid.Row="2" Fill="{StaticResource BorderFill}"/>
        <Rectangle Grid.Column="2" Grid.Row="0" Fill="{StaticResource BorderFill}"/>
        <Rectangle Grid.Column="2" Grid.Row="1" Fill="{StaticResource BorderFill}"/>
        <Rectangle Grid.Column="2" Grid.Row="2" Fill="{StaticResource BorderFill}"/>
        <GridSplitter Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="8" Background="#00000000"/>
        <GridSplitter Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="8" Background="#00000000"/>
        <GridSplitter Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="8" Background="#00000000"/>
        <GridSplitter Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Height="8" Background="#00000000"/>
        <Rectangle Grid.Column="1" Grid.Row="1" Fill="{StaticResource BorderFill}" HorizontalAlignment="Left" VerticalAlignment="Top" Cursor="SizeNWSE" Tag="TopLeft" Width="8" Height="8"/>
        <Rectangle Grid.Column="1" Grid.Row="1" Fill="{StaticResource BorderFill}" HorizontalAlignment="Right" VerticalAlignment="Top" Cursor="SizeNESW" Tag="TopRight" Width="8" Height="8"/>
        <Rectangle Grid.Column="1" Grid.Row="1" Fill="{StaticResource BorderFill}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Cursor="SizeNESW" Tag="BottomLeft" Width="8" Height="8"/>
        <Rectangle Grid.Column="1" Grid.Row="1" Fill="{StaticResource BorderFill}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" Tag="BottomRight" Width="8" Height="8"/>
        <Label Content="{Binding RatioText}" Grid.ColumnSpan="3" Grid.RowSpan="3" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="Black">
            <Label.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="1" Y="1"/>
                </TransformGroup>
            </Label.RenderTransform>
        </Label>
        <Label Content="{Binding RatioText}" Grid.ColumnSpan="3" Grid.RowSpan="3" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="White"/>
    </Grid>
</UserControl>
